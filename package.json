{
  "name": "openoxen",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "OpenOxen: Attractor + Coding Agent Loop implementation",
  "bin": {
    "openoxen": "./bin/openoxen.js"
  },
  "scripts": {
    "cli": "node --experimental-strip-types src/cli/main.ts",
    "test": "node --experimental-strip-types --test tests/**/*.test.ts",
    "test:watch": "node --experimental-strip-types --test --watch tests/**/*.test.ts"
  },
  "dependencies": {
    "@mariozechner/pi-ai": "^0.55.3"
  }
}
